name: psautohint

on:
  workflow_dispatch:

jobs:
  package:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: clone
        run: |
          git clone https://aur.archlinux.org/psautohint.git

name: _python-injector

on:
  workflow_call:
    inputs:
      ref:
        type: string
        default: $GITHUB_REF_NAME
  workflow_dispatch:

jobs:
  process:
    uses: highkeep/aur/.github/workflows/template_aur.yml@main
    with:
      ref: ${{ inputs.ref }}
      repo_url: https://aur.archlinux.org
      repo_pkg: python-injector 
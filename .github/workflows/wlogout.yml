name: wlogout

on:
  workflow_dispatch:
  schedule:
    - cron: "0 7,19 * * *"

jobs:
  wlogout:
    uses: highkeep/pkgbuild/.github/workflows/multiarch_builder.yml@v2.2
    with:
      repoUrl: https://github.com/highkeep/wlogout.git
      repoPkg: wlogout
      nvidiaUtils: true
      recvKeys: E25D679AF73C6D2F
      art: '[{"architecture":"alderlake", "repoTag":"alderlake"},{"architecture":"generic", "repoTag":"generic_x86_64"}]'
